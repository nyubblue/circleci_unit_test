version: 2.1
jobs:
  say-hello:
    docker:
      - image: circleci/python
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: echo "hello"

workflows:
  say-hello-workflow:
    jobs:
      - say-hello
